# 🧠 DocuMind AI

**DocuMind AI**는 사용자가 업로드한 문서(PDF)나 텍스트를 기반으로 정확한 정보를 제공하는 **RAG(검색 증강 생성) 기반 챗봇 시스템**입니다.
이 시스템은 RAG 기술이 적용된 답변과 일반 LLM(GPT-4)의 답변을 비교하여, 지식베이스의 중요성을 시각적으로 확인할 수 있도록 돕습니다.

---

## 🚀 프로젝트 개요 (Overview)

이 프로젝트는 최신 AI 기술 스택을 활용하여 구축되었습니다. 사용자는 지식베이스를 쉽게 구축하고, AI가 이를 어떻게 활용하는지 실시간으로 파악할 수 있습니다.

### ✨ 주요 기능
- **다양한 소스 지원:** PDF 파일 업로드 및 텍스트 직접 입력 지원.
- **RAG vs LLM 비교:** 버튼 하나로 RAG 검색 모드와 일반 GPT 모드를 즉시 전환하여 비교 가능.
- **투명한 출처 제공:** 답변 생성 시 참고한 문서의 이름과 페이지 번호를 정확히 표시.
- **최신 고성능 모델:** OpenAI **GPT-4** 모델 탑재.
- **벡터 검색:** MongoDB Atlas Vector Search를 활용한 의미 기반 검색.

### 🛠️ 기술 스택 (Tech Stack)
- **Frontend:** Streamlit (UI/UX)
- **Backend:** FastAPI (REST API Server)
- **AI/ML:** LangChain, OpenAI GPT-4, text-embedding-3-small
- **Database:** MongoDB Atlas (Vector Search)
- **Deployment:** Docker & Docker Compose

---

## 📋 필수 프로그램 및 준비 사항 (Prerequisites)

이 프로젝트를 실행하기 위해 다음 도구들이 필요합니다.

### 1. 필수 프로그램
- **Docker Desktop** (추천): 가장 간편한 실행 방법입니다.
- *(선택 사항: 로컬 개발 시)* **Python 3.10+** 및 **uv** (패키지 매니저)

### 2. API 키 설정 (필수)
프로젝트 실행 전, 최상위 경로에 `.env` 파일을 생성하고 다음 정보를 입력해야 합니다.

```env
OPENAI_API_KEY=sk-your-openai-api-key-here
MONGO_URI=mongodb+srv://user:pass@cluster0.abcde.mongodb.net/?retryWrites=true&w=majority
```
> **주의:** MongoDB Atlas에는 반드시 `rag_db` 데이터베이스의 `embeddings` 컬렉션에 **1536차원(cosine)**의 Vector Search Index (`vector_index`)가 생성되어 있어야 합니다.

---

## 🏃‍♂️ 실행 방법 (How to Run)

### 방법 1. Docker로 바로 실행하기 (추천) 🐳
어떤 환경에서든 동일하게 실행됩니다.

1. 터미널을 열고 프로젝트 폴더로 이동합니다.
2. 다음 명령어를 실행합니다.
   ```powershell
   docker-compose up --build
   ```
3. 웹 브라우저에서 **[http://localhost:8501](http://localhost:8501)** 로 접속합니다.

### 방법 2. 로컬 개발 환경에서 실행하기 💻
개발 및 수정을 위해 각각 실행할 때 사용합니다.

1. **의존성 설치:**
   ```powershell
   uv sync
   ```
2. **백엔드 실행 (터미널 1):**
   ```powershell
   uv run uvicorn app.backend.main:app --reload
   ```
3. **프론트엔드 실행 (터미널 2):**
   ```powershell
   uv run streamlit run app/frontend/ui.py
   ```

---

## 📁 프로젝트 구조 (Project Structure)
```
rag_project/
├── app/
│   ├── backend/
│   │   ├── main.py      # FastAPI 진입점
│   │   ├── api.py       # API 엔드포인트 router
│   │   ├── rag.py       # RAG 로직 (Load, Split, Embed, Search)
│   │   └── db.py        # MongoDB 연결 설정
│   └── frontend/
│       └── ui.py        # Streamlit 채팅 UI
├── data/                # 업로드된 임시 파일 저장소
├── .env                 # 환경 변수 (API Key 등)
├── docker-compose.yml   # Docker 오케스트레이션 설정
├── backend.Dockerfile   # 백엔드 이미지 빌드 설정
└── frontend.Dockerfile  # 프론트엔드 이미지 빌드 설정
```
